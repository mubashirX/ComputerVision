# Ignore video file types requested
*.mp4
*.avi

# Note: .gitignore cannot ignore files based on size (e.g., >70 MB).
# To prevent committing large files consider using Git LFS or a pre-commit hook.
Classification/dataset_split/
Segmentation/data/
Segmentation/visualized_labels/